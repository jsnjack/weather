<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Background location lets the widget get a *current* fix during a
         WorkManager refresh while travelling, instead of reusing a stale
         home-city fix. Opt-in: requested only if you pick Auto mode. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:name=".RainWidgetApp"
        android:label="@string/app_name"
        android:icon="@drawable/ic_widget_preview"
        android:roundIcon="@drawable/ic_widget_preview"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Must stay exported to receive the system APPWIDGET_UPDATE
             broadcast. The manual-refresh action deliberately lives on the
             non-exported ManualRefreshReceiver below, so no other app can
             trigger refresh work. -->
        <receiver
            android:name=".RainWidgetProvider"
            android:exported="true"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/rain_widget_info" />
        </receiver>

        <!-- Manual refresh (tap the timestamp pill). Not exported: the in-app
             PendingIntent targets it explicitly, which a same-UID component is
             allowed to do without being exported. -->
        <receiver
            android:name=".ManualRefreshReceiver"
            android:exported="false" />

        <activity
            android:name=".ConfigureActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault.Dialog">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

    </application>
</manifest>
